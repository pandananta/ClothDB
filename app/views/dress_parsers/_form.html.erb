<%= form_for(@dress_parser) do |f| %>
  <% if @dress_parser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dress_parser.errors.count, "error") %> prohibited this dress_parser from being saved:</h2>

      <ul>
      <% @dress_parser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
